import { colors } from "@/components/constants/colors";
import { ScrollView } from "@gluestack-ui/themed";
import React from "react";

export default function Trips() {
  return (
    <>
      <ScrollView bgColor={colors.background}></ScrollView>
    </>
  );
}
