import { colors } from "@/components/constants/colors";
import { Box } from "@gluestack-ui/themed";
import React from "react";

export default function Reservation() {
  return <Box flex={1} bgColor={colors.background}></Box>;
}
